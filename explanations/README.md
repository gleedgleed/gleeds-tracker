# Check notes

Each file in this folder is the per-check explanation shown in the right pane
of the Textual UI. Filename = `<check name>.md` (exact match including spaces),
content is Markdown.

If a file doesn't exist for a given check, the UI shows a placeholder.
